{
  "name": "braa",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Bra! — a one-tap dog-training timing game (v2). Phase 1: the perfect single mark.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1",
    "@types/node": "^22.10.5",
    "typescript": "^5.7.2",
    "vite": "^6.0.7",
    "vitest": "^2.1.8"
  },
  "dependencies": {
    "@babylonjs/core": "^7"
  }
}
